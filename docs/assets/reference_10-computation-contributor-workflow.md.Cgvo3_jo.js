import{_ as t,o as a,c as o,a4 as n}from"./chunks/framework.CzJ_HCFe.js";const u=JSON.parse('{"title":"Add or change a computation safely","description":"","frontmatter":{},"headers":[],"relativePath":"reference/10-computation-contributor-workflow.md","filePath":"reference/10-computation-contributor-workflow.md","lastUpdated":1788414836000}'),i={name:"reference/10-computation-contributor-workflow.md"};function r(s,e,d,c,l,p){return a(),o("div",null,[...e[0]||(e[0]=[n(`<h1 id="add-or-change-a-computation-safely" tabindex="-1">Add or change a computation safely <a class="header-anchor" href="#add-or-change-a-computation-safely" aria-label="Permalink to &quot;Add or change a computation safely&quot;">​</a></h1><blockquote><p><strong>Audience:</strong> contributors and maintainers implementing a computation change. <strong>Status:</strong> current contribution workflow; it does not authorize frozen-core changes. <strong>Owners:</strong> <code>AGENTS.md</code>, <code>ARCHITECTURE.md</code>, the computation inventory, and the verification tools. <strong>Review when:</strong> any of those contracts, public surfaces, or release rules change.</p></blockquote><p>This is the concrete path from a proposed formula or rule to a reviewable pull request. It applies to astronomical facts, Panchangam classifications, textual rules, personal calculations, scores, and generated interpretations.</p><p>Use the <a href="./computation-record-template">copyable worksheet and JSON record</a> while implementing. The canonical machine-readable catalogue is <a href="computations.json"><code>computations.json</code></a>; its vocabulary and required fields are defined by <a href="computations.schema.json"><code>computations.schema.json</code></a>.</p><h2 id="_1-stop-before-choosing-a-file" tabindex="-1">1. Stop before choosing a file <a class="header-anchor" href="#_1-stop-before-choosing-a-file" aria-label="Permalink to &quot;1. Stop before choosing a file&quot;">​</a></h2><p>First classify the requested change. A pull request is not permission to cross the repository&#39;s frozen boundaries.</p><table tabindex="0"><thead><tr><th>Change</th><th>Correct owner</th><th>May proceed without new owner approval?</th></tr></thead><tbody><tr><td>Change a Drik, Surya Siddhanta, or Vakya formula, engine finalization, muhurta-window owner, or festival evaluation mechanism</td><td><code>telugu_panchangam/engines/</code></td><td><strong>No. Stop and obtain explicit owner approval.</strong></td></tr><tr><td>Append one festival row using an existing deciding moment</td><td>Existing rule table in <code>engines/base.py</code></td><td>Yes, but only with a precise external reference and a new reference-verified test; do not change an existing assertion</td></tr><tr><td>Add a deterministic fact derived from engine output</td><td>A new focused top-level module under <code>telugu_panchangam/</code></td><td>Yes; consume the public engine result and keep the engines unchanged</td></tr><tr><td>Add a birth-dependent or electional computation</td><td>A new or existing focused module under <code>telugu_panchangam/personal/</code></td><td>Yes; consume engine outputs and keep personal inputs out of public URLs</td></tr><tr><td>Add another domain such as Gochara</td><td>Its focused domain package</td><td>Yes when additive; do not move unrelated core logic merely for symmetry</td></tr><tr><td>Expose an existing computation through MCP</td><td><code>mcp/tools.py</code> plus <code>mcp/server.py</code> signature/help</td><td>Yes, with parity tests and a PyPI version bump</td></tr><tr><td>Present an existing result in the browser</td><td><code>src/</code> adapter/panel code or generated shared data</td><td>Yes, but do not invent a second owner; screenshots and owner sign-off are required before pushing UI changes</td></tr><tr><td>Change the subscriber-facing ICS representation</td><td><code>generators/ics.py</code></td><td><strong>No. Stop and obtain explicit owner approval.</strong></td></tr><tr><td>Change deployment, Pages composition, CNAME, or workflow triggers</td><td><code>.github/workflows/</code></td><td><strong>No. Stop and obtain explicit owner approval.</strong></td></tr><tr><td>Make a new feature pass by changing an existing expected calculation</td><td>Existing test assertion</td><td><strong>No. Stop.</strong> Treat this as a frozen-core or correction proposal and show the independent evidence first</td></tr></tbody></table><p>Adapters serialize, validate inputs, and choose presentation. They must not own domain formulas. If Python already owns the computation, prefer generated shared tables or explicit parity fixtures over manually retyping constants into TypeScript.</p><h2 id="_2-define-the-contract-before-implementation" tabindex="-1">2. Define the contract before implementation <a class="header-anchor" href="#_2-define-the-contract-before-implementation" aria-label="Permalink to &quot;2. Define the contract before implementation&quot;">​</a></h2><p>Open or update one GitHub story and record:</p><ul><li>a stable computation ID that describes the meaning, not its file path;</li><li>formula or textual rule in plain language;</li><li>inputs, defaults, accepted ranges, units, coordinate frame, system, ayanamsa, timezone, and day-boundary convention;</li><li>outputs, units, rounding, null/error behavior, and public consumers;</li><li>edge cases, known disagreements, safety limitations, and backward- compatibility expectations;</li><li>evidence class and the exact evidence needed for the intended assurance statement; and</li><li>which Python, MCP, browser, generated-data, or ICS surfaces must agree.</li></ul><p>Start one feature branch for that story. If investigation uncovers a different defect, source conflict, or frozen-core correction, open a separate story and PR rather than silently widening the branch.</p><h2 id="_3-plan-evidence-at-the-right-level" tabindex="-1">3. Plan evidence at the right level <a class="header-anchor" href="#_3-plan-evidence-at-the-right-level" aria-label="Permalink to &quot;3. Plan evidence at the right level&quot;">​</a></h2><p>Regression tests establish reproducibility. They do not independently verify a formula or textual rule.</p><table tabindex="0"><thead><tr><th>Claim type</th><th>Required evidence before calling it verified</th></tr></thead><tbody><tr><td>Drik astronomical value or boundary</td><td>Same date, place, timezone, ayanamsa and convention compared with the named Drik Panchang day page; use several dates and more than one city, and record values plus tolerance</td></tr><tr><td>Surya Siddhanta or Vakya result</td><td>An edition, almanac, or independently implemented model that uses that system; a modern Drik result is not the authority for an intentional system difference</td></tr><tr><td>Textual or electional rule</td><td>Identified edition plus chapter/verse/page locator, with a criterion-by-criterion crosswalk and conflicts disclosed</td></tr><tr><td>Regional convention</td><td>Named regional or lineage scope; do not present it as universal</td></tr><tr><td>Product ranking or heuristic</td><td>Mark it <code>project_heuristic</code> / <code>heuristic</code>, explain the choice, and test deterministic behavior</td></tr><tr><td>Generated interpretation</td><td>Verify cited structured facts separately; keep prose and advice under the generated-interpretation boundary</td></tr></tbody></table><p>If the needed source cannot be inspected, use the honest state such as <code>needs_locator</code>, <code>partially_verified</code>, <code>engine_pinned</code>, or <code>heuristic</code>. Never upgrade the state because a test passes or because the implementation and its own output agree.</p><h2 id="_4-write-tests-then-add-the-owner" tabindex="-1">4. Write tests, then add the owner <a class="header-anchor" href="#_4-write-tests-then-add-the-owner" aria-label="Permalink to &quot;4. Write tests, then add the owner&quot;">​</a></h2><p>Add a focused test that fails because the new behavior is absent. Do not edit an existing calculation assertion to make the feature fit.</p><p>The test set should cover, as applicable:</p><ul><li>normal and boundary inputs, invalid input, rollover, timezone and DST;</li><li>multiple dates and cities, including a non-IST location for local-time code;</li><li>every calculation system and ayanamsa the public signature accepts;</li><li>exact table membership, units, rounding, interval inclusion/exclusion, and zero-duration or missing-event behavior;</li><li>independent comparison cells with source URL/locator and tolerance;</li><li>Python/MCP serialization and browser parity when the result crosses those surfaces; and</li><li>backward compatibility for existing public fields, signatures, feed bytes, or accepted aliases.</li></ul><p>Put the computation in the narrowest additive owner from the table above. Keep the formula there. Adapters may call it but must not reinterpret its constants or silently apply a different time basis.</p><h2 id="_5-update-the-documentation-contract-in-the-same-pr" tabindex="-1">5. Update the documentation contract in the same PR <a class="header-anchor" href="#_5-update-the-documentation-contract-in-the-same-pr" aria-label="Permalink to &quot;5. Update the documentation contract in the same PR&quot;">​</a></h2><ol><li>Add or update the record in <a href="computations.json"><code>computations.json</code></a>.</li><li>Add or update claim-level evidence in <a href="provenance.json"><code>provenance.json</code></a>; keep source locators and assurance states precise.</li><li>Update the appropriate engine or derived-computation reference page and any affected MCP/browser/public-surface documentation.</li><li>Add every material mirror to <code>implementations</code> with role <code>mirror</code>; a serializer-only adapter normally remains a coverage exclusion.</li><li>Record limitations even when the algorithm is fully deterministic.</li><li>Regenerate <a href="project-facts.json"><code>project-facts.json</code></a> if a system, city, MCP tool, computation record, or activity catalogue changed.</li></ol><p>Run:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tools/check_computation_inventory.py</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tools/check_documentation_freshness.py</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --write</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # only when facts changed</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tools/verify_project.py</span></span></code></pre></div><p>Review the generated fact diff; do not accept it merely because it is generated. The complete verifier is offline and must pass before commit.</p><h2 id="_6-review-public-surfaces-and-release-impact" tabindex="-1">6. Review public surfaces and release impact <a class="header-anchor" href="#_6-review-public-surfaces-and-release-impact" aria-label="Permalink to &quot;6. Review public surfaces and release impact&quot;">​</a></h2><p>Before opening the PR, reconcile every selected surface:</p><ul><li><strong>Python:</strong> one canonical owner and a stable return contract;</li><li><strong>MCP:</strong> signature, help text, validation, JSON output, package README, tests, <code>CHANGELOG.md</code>, and synchronized <code>pyproject.toml</code> / <code>server.json</code> patch or minor version;</li><li><strong>Browser:</strong> same vocabulary, formula, timezone, system and ayanamsa; representative desktop/mobile screenshots and owner sign-off before push;</li><li><strong>Generated data:</strong> source, generator, committed/ignored status, and a drift check;</li><li><strong>ICS:</strong> no format change without explicit owner approval and byte-level regression evidence; and</li><li><strong>GitHub Pages/workflows:</strong> no workflow, CNAME, or layered-artifact change without explicit owner approval.</li></ul><p>The PR description must name the computation IDs, evidence inspected, exact dates/cities/systems compared, tolerances, public surfaces, compatibility impact, commands run, and any approval still required. Keep the branch unmerged for owner review.</p><h2 id="worked-example-five-limb-panchanga-shuddhi" tabindex="-1">Worked example: five-limb Panchanga Shuddhi <a class="header-anchor" href="#worked-example-five-limb-panchanga-shuddhi" aria-label="Permalink to &quot;Worked example: five-limb Panchanga Shuddhi&quot;">​</a></h2><p>The existing <code>derived.panchanga-shuddhi</code> record demonstrates the additive path without rewriting an engine:</p><ol><li><strong>Classification:</strong> it consumes the five limbs of <code>PanchangamDay</code>, so the owner is the additive module <code>telugu_panchangam/panchanga_shuddhi.py</code>, not an engine or MCP adapter.</li><li><strong>Contract:</strong> <code>assess_shuddhi</code> grades Tithi, Vaaram, Nakshatra, Yoga and Karana at sunrise, returning five assessments, a count from 0–5, and a summary verdict.</li><li><strong>Evidence:</strong> it is a textual rule. The record honestly remains <code>needs_locator</code>; deterministic tests do not turn the table into an independently verified textual claim.</li><li><strong>Tests:</strong> <code>tests/test_panchanga_shuddhi.py</code> covers the limb tables, verdict count, day extraction and MCP output. A source-verification PR would add an edition/locator and exact table crosswalk without rewriting passing behavior.</li><li><strong>Surfaces:</strong> Python owns the result and MCP exposes it. It has no browser or ICS mirror, so the record does not claim those surfaces.</li><li><strong>Release:</strong> the MCP tool required synchronized help/package documentation, tests, changelog entry and version bump. No engine, ICS, or workflow change was necessary.</li></ol><p>That is a complete, reviewable record even though its textual evidence remains open: method, ownership, tests, surfaces, limitation, and actual assurance state are all explicit.</p>`,34)])])}const m=t(i,[["render",r]]);export{u as __pageData,m as default};
