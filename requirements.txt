# Thin shim — pyproject.toml is the source of truth for runtime + test deps.
# This file exists so that `pip install -r requirements.txt` keeps working
# in CI and local-dev muscle memory. The editable install picks up every
# direct dep (mcp, pyswisseph, pytz, geopy, timezonefinder, icalendar) plus
# the [test] extra (pytest and the Ruff quality gate). Playwright is installed
# separately in CI.
-e .[test]
