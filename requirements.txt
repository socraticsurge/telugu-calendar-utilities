# Compatibility shim — pyproject.toml is the dependency declaration and
# uv.lock is the reproducible CI/release resolution. This file keeps
# `pip install -r requirements.txt` available for local-dev muscle memory.
# The editable install picks up every
# direct dep (mcp, pyswisseph, pytz, geopy, timezonefinder, icalendar) plus
# the [test] extra (pytest and the Ruff quality gate). CI selects the locked
# [browser-test] extra for Playwright, then installs Chromium separately.
-e .[test]
